# signal-logs/ — Obsolete

The legacy flat-file JSON store has been removed. Signals are
persisted exclusively in the `Signal` table in PostgreSQL
(`prisma/schema.prisma`).

This directory and its JSON files are kept temporarily for reference
and will be deleted in a future release.
